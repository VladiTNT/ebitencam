# ebitencam

**ebitencam** is library that provides a basic 2D camera for ebitengine. It has basic translation features and even functions that allow for a smooth following effect.

This is also a part of my other ebiten library called **ebitenplus** but I decided to split all of the main components of that library into their own libraries.